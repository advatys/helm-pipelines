 {{- if eq .Values.secrets.git.generate "true" }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secrets.git.name }}
  annotations:
    argocd.argoproj.io/sync-options: Prune=false
type: Opaque
stringData:
  GIT_USERNAME: "git_username"
  GIT_TOKEN: "git_token" 
 {{- end }}